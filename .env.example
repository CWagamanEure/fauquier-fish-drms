# Local development only. Copy to .env; never commit real credentials.
DB_NAME=fishdb
DB_USER=fishdb
DB_PASSWORD=fishdb
DB_ROOT_PASSWORD=change-this-local-root-password
APP_PORT=8080
PHPMYADMIN_PORT=8081
# Optional: path to a database export, used ONLY on first initialization.
# DB_SEED_FILE=./.local/docker-seed/001-fishdb.sql
